# Plano - Impressao local QZ Tray

1. Cobrir recibo, selecao, conexao, falha e bloqueio duplicado com testes unitarios.
2. Adicionar `qz-tray` e um adaptador browser-only para WebSocket, descoberta e HTML pixel.
3. Integrar estado local e acoes de impressao no painel, preservando a fila do agente.
4. Adicionar recibo isolado para fallback nativo e estilos responsivos do painel.
5. Validar testes, lint, typecheck, build, navegador local e conexao QZ real.
6. Documentar instalacao, permissao, diagnostico e limites; atualizar o Segundo Cerebro.
7. Publicar somente depois da verificacao local e validar o painel em producao.
